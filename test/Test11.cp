def int gcd(int a, int b)
    if (a==b) then
        return (a)
    fi;
    if (a>b) then
        return(gcd(b,b))
    else
        return(gcd(a,b))
    fi;
fed;
int a; int b;
print gcd(21,15);
a = b + 9 * a;
print 45.555;
print 2*(gcd(21, 28) + 6).
